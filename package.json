{
  "name": "discord-logging-bot",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:bot": "npm run dev --workspace=@logger/bot",
    "dev:dashboard": "npm run dev --workspace=@logger/dashboard",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.1"
  }
}
